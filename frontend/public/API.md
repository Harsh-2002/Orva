# Orva API

The dashboard no longer renders this file. Use the in-app **Docs** page
at `/docs` for copy-ready examples that use the current instance URL.

The canonical HTTP reference lives in `docs/API.md` at the repository
root.
